# Webfrontend entry of __PLUGIN_NAME__, compiled + bundled into
# __PLUGIN_NAME__.js by fylr-build-plugin (build.yml webfrontend.coffee1).

class __PLUGIN_PASCAL__
    @hello: ->
        "__PLUGIN_NAME__"
